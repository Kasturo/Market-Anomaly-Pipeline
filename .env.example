# Copy this file to ".env" in the same folder as pipeline.py and fill in values.
# Never commit .env (it is listed in .gitignore).
#
# Used by pipeline.py so boto3 can upload to S3 without re-exporting in every shell.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
